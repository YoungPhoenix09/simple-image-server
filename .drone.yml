---
kind: pipeline
type: exec
name: java-pipeline

platform:
  os: darwin
  arch: amd64

steps:
  - name: test
    commands:
#     - gradle wrapper
      - pwd
      - ls
      - ls /
      - ./setuplocal.sh
      - ./gradlew test
#  - name: comment-pr
#    image: maniator/gh
#    commands:
#      - gh issue {pr#} comment --body "@dependabot squash and merge"
#    environment:
#      GH_TOKEN:
#        from_secret: gh_token
