---
kind: pipeline
type: docker
name: java-pipeline

steps:
  - name: test
    image: dtforce/dind-gradle:v7.4.2-alp-jdk17-py
    commands:
      - gradle wrapper
      - ./setuplocal.sh
      - ./gradlew test
#  - name: comment-pr
#    image: maniator/gh
#    commands:
#      - gh issue {pr#} comment --body "@dependabot squash and merge"
#    environment:
#      GH_TOKEN:
#        from_secret: gh_token
